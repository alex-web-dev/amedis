include ../mixins/_mixins.pug

block vars

doctype html
html(lang="ru")
    head
        title
            block title
        meta(charset='utf-8')
        meta(name='viewport' content='width=device-width, initial-scale=1')
        meta(name='format-detection' content='telephone=no')
        link(rel='shortcut icon' href='assets/img/favicon.png')
        link(rel='stylesheet' href='assets/css/vendors.css?v=' + Math.round(Math.random() * 100000000))
        link(rel='stylesheet' href='assets/css/app.css?v=' + Math.round(Math.random() * 100000000))

    body
        include ../partials/_header.pug

        block content

        include ../partials/_footer.pug

        block modals

        include ../modals/_modals.pug

        block scripts
        script(src='assets/js/vendors.js?v=' + Math.round(Math.random() * 100000000))
        script(src='assets/js/app.js?v=' + Math.round(Math.random() * 100000000))